from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='e2e machine learning workflow [tech stack: git, dvc, dvclive, mlflow, fastapi, pytest, github actions, docker]',
    author='ronilpatil',
    license='MIT',
)
